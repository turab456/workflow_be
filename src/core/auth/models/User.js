/**
 * DEPRECATED — Local User model removed.
 *
 * User identity and profile management is owned by Docqube.
 * This workflow engine does not maintain a local user table.
 * User context is derived entirely from the JWT payload at request time.
 */
