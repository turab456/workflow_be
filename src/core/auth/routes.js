/**
 * DEPRECATED — Local auth has been removed.
 *
 * Authentication and user management is handled entirely by Docqube.
 * This workflow engine accepts JWTs issued by Docqube and verifies them
 * using the shared JWT_SECRET.
 *
 * This file is intentionally empty. The /api/v1/auth route is no longer
 * mounted in app.js.
 */
