/**
 * DEPRECATED — ContractRequest model removed.
 *
 * Business record storage is owned by Docqube.
 * This engine only persists workflow execution state via:
 *   - WorkflowDefinition  — BPMN process definitions per tenant
 *   - WorkflowInstance    — live process instance tracking
 */
